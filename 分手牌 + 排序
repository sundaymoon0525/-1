def separate_sort(cards): 
    global pn
    hands = []
    for i in range(pn):
        hands.append(cards[i*5: i*5+5])
    for i in hands:
        i.sort(key = lambda x: int(x[1]))
    return hands 
